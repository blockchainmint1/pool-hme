# TXC explorer backend. Node (texitcoind) runs on the HOST via node-spinner.
# Frontend points at https://${DOMAIN}/api + wss://${DOMAIN}/api/v1/ws.

services:
  # electrs: host networking so it can hit 127.0.0.1:RPC_PORT / P2P_PORT
  # on the host where texitcoind listens. Bind-mounts the node datadir
  # read-only for blocks/.
  electrs:
    image: getumbrel/electrs:latest
    container_name: txc-electrs
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./data/electrs:/data
      - /var/lib/texitcoin:/bitcoin:ro
    command:
      - --network=bitcoin
      - --daemon-rpc-addr=127.0.0.1:${RPC_PORT}
      - --daemon-p2p-addr=127.0.0.1:${P2P_PORT}
      - --daemon-dir=/bitcoin
      - --db-dir=/data
      - --electrum-rpc-addr=127.0.0.1:50001
      - --http-addr=127.0.0.1:3000
      - --cookie=${RPC_USER}:${RPC_PASSWORD}
      - --log-filters=INFO

  mempool-db:
    image: mariadb:11
    container_name: txc-mempool-db
    restart: unless-stopped
    environment:
      MARIADB_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MARIADB_DATABASE: ${MYSQL_DATABASE}
      MARIADB_USER: ${MYSQL_USER}
      MARIADB_PASSWORD: ${MYSQL_PASSWORD}
    volumes:
      - ./data/mempool-db:/var/lib/mysql
    expose:
      - "3306"

  mempool-api:
    image: mempool/backend:latest
    container_name: txc-mempool-api
    restart: unless-stopped
    depends_on:
      - mempool-db
    user: "1000:1000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      MEMPOOL_BACKEND: "electrum"
      ELECTRUM_HOST: "host.docker.internal"
      ELECTRUM_PORT: "50001"
      ELECTRUM_TLS_ENABLED: "false"
      CORE_RPC_HOST: "host.docker.internal"
      CORE_RPC_PORT: "${RPC_PORT}"
      CORE_RPC_USERNAME: ${RPC_USER}
      CORE_RPC_PASSWORD: ${RPC_PASSWORD}
      DATABASE_ENABLED: "true"
      DATABASE_HOST: "mempool-db"
      DATABASE_DATABASE: ${MYSQL_DATABASE}
      DATABASE_USERNAME: ${MYSQL_USER}
      DATABASE_PASSWORD: ${MYSQL_PASSWORD}
      STATISTICS_ENABLED: "true"
      MEMPOOL_NETWORK: "mainnet"
      MEMPOOL_INDEXING_BLOCKS_AMOUNT: "-1"
    volumes:
      - ./data/mempool-cache:/backend/cache
    expose:
      - "8999"

  nginx:
    image: nginx:alpine
    container_name: txc-nginx
    restart: unless-stopped
    depends_on:
      - mempool-api
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./data/certbot/conf:/etc/letsencrypt:ro
      - ./data/certbot/www:/var/www/certbot:ro
    environment:
      DOMAIN: ${DOMAIN}
    command: >
      /bin/sh -c "envsubst '$$DOMAIN' < /etc/nginx/nginx.conf > /tmp/nginx.conf
      && exec nginx -c /tmp/nginx.conf -g 'daemon off;'"

  certbot:
    image: certbot/certbot:latest
    container_name: txc-certbot
    restart: unless-stopped
    volumes:
      - ./data/certbot/conf:/etc/letsencrypt
      - ./data/certbot/www:/var/www/certbot
    entrypoint: >
      /bin/sh -c "trap exit TERM;
      while :; do certbot renew --webroot -w /var/www/certbot --quiet;
      sleep 12h & wait $${!}; done"
