# nicehash-proxy env
# Public rental port miners/verifiers connect to.
LISTEN_PORT=3533
# Real stratum (same box).
UPSTREAM_HOST=127.0.0.1
UPSTREAM_PORT=3433
# Difficulty advertised + enforced for rental connections.
# NiceHash requires >= 50000. 65536 is verified working on this pool.
MIN_DIFF=65536
# LTC payout address used by the internal job-cache connection.
SENTINEL_USER=ltc1qfvefpfgmznsvzwkczk47hq2mt5d07qwqyh05de
